name: StackIQ Tyler iDS Group E

on:
  schedule:
    - cron: "30 8 * * *"
  workflow_dispatch: {}

jobs:

  ector:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Ector
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Ector"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  taylor:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Taylor
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Taylor"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  randall:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Randall
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Randall"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  lavaca:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Lavaca
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Lavaca"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  calhoun2:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Calhoun2
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Calhoun2"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  panola:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Panola
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Panola"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  scurry:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Scurry
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Scurry"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  washington:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Washington
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Washington"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  pecos:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Pecos
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Pecos"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  dallam:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Dallam
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Dallam"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
