name: StackIQ Tyler iDS Group D

on:
  schedule:
    - cron: "30 8 * * *"
  workflow_dispatch: {}

jobs:

  potter:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Potter
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Potter"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  andrews:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Andrews
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Andrews"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  palopinto:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape PaloPinto
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "PaloPinto"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  wise:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Wise
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Wise"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  bowie:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Bowie
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Bowie"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  polk:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Polk
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Polk"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  winkler:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Winkler
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Winkler"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  yoakum:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Yoakum
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Yoakum"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  somervell:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Somervell
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Somervell"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  hamilton:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Hamilton
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Hamilton"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  mills:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape Mills
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "Mills"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
  mclennan:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - run: pip install -r scraper/requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Scrape McLennan
        run: python scraper/tyler_scraper_universal.py
        env:
          COUNTY: "McLennan"
          LOOKBACK_DAYS: "3"
          MAX_PAGES: "5"
          MAX_RECORDS: "500"
          WALL_MINUTES: "75"
      - name: Push to DB
        run: python scraper/push_to_db.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
